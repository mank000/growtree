# 🌱 Леший Bot

Чат-бот-планировщик, который превращает выполнение задач в рост виртуального леса.  
Геймификация, визуальный прогресс — всё, чтобы формировать полезные привычки без давления.

## 🎯 Возможности

- **Управление задачами** — создание, удаление, завершение задач  
- **Забота о природе** — каждое выполненное действие даёт дереву +1 рост    
- **Напоминания** — бот напоминает о задачах в нужное время  
- **Прогресс** — статистика выполнения задач и динамика роста дерева  
- **Вариативные ответы** — живой бот с эмоциональной обратной связью  

## 🛠 Технологии

- **Backend**: Python 3.13
- **Framework**: maxapi 
- **Database**: PostgreSQL  
- **ORM**: Tortoise ORM 
- **Containerization**: Docker  
- **APIs**: Max Bot API  

## 📋 Требования

- Python 3.11+  
- PostgreSQL 15
- Max Bot Token  
- Docker (рекомендуется для запуска)  
- Poetry/venv  

## 🚀 Быстрый старт

### 1. Клонирование проекта и установка зависимостей

```bash
git clone https://github.com/<your-username>/growtask-bot
cd __название__-bot

# Если используете Poetry:
poetry install

# Или через pip:
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создай файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте .env, указав ваш MAX Bot Token и параметры базы данных (например, DATABASE_URL=postgresql://user:password@localhost/growtask_db).

### 3. Запуск через Docker (рекомендуется)

```bash
docker-compose up -d

docker-compose logs -f bot

docker-compose down
```

## 📱 Использование бота

После настройки, найди бота в MAX и активируй его


### Основные команды:

- `/plant` - посадить дерево
- `/tasks` - список задач
- `/profile` - профиль и статистика


2. **Тестирование в MAX:**
   - Отправь команды боту
   - Проверь логи: `docker compose logs -f bot`
   - Убедись, что все команды работают



## 👥 Команда

МАКСИМАЛОЧКА | Hackathon Team 309 - VK Education Hackathon 2025

## 🙏 Благодарности

- VK Education за возможность участия
- MAX за отличный API и документацию



